<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <h1>
      Department #<%= @the_department %> details
    </h1>

    <div class="row mb-3">
      <div class="col">
        <a href="/departments">
          Go back
        </a>
      </div>
    </div>

    <dl>
      <dt>
        Name
      </dt>
      <dd>
        <%= department = Department.where ({ :id => @the_department.ids }) %>
      </dd>

      <dt>
        Number of courses
      </dt>
      <dd>
        <%= Course.where({ :id => @the_department.name }).count %> 
      </dd>

    </dl>
  </div>
</div>

<hr>

<h2>Courses</h2>
<% a_course = Course.where({ :id => @the_department.name }).each do |p| %>
<ul>
  <%  @the_department.each do |a_course| %>
    <li>
      <a href="/courses/<%= course.name %>"><%= course.count %></a>
    </li>
 <%end%>
<ul>
<%end%>
